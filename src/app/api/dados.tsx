// pages/api/dados.ts
/*
import type { NextApiRequest, NextApiResponse } from "next";

const caminhoArquivo = "@/../public/dados.json";

export default function handler(req: NextApiRequest, res: NextApiResponse) {
	// Realize as operações de leitura e escrita aqui
	const dados = []; // Substitua isso com a lógica adequada para ler os dados do arquivo

	res.status(200).json(dados);
}
*/
